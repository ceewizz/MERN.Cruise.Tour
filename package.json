{
  "name": "mern.cruise.tour",
  "version": "1.0.0",
  "description": "Project 3: MERN Stack Single-Page Application",
  "main": "server/server.js",
  "scripts": {
    "start": "node server",
    "develop": "concurrently \"cd server && node server\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm i && npm run build",
    "render-build":"npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"

  }
}
